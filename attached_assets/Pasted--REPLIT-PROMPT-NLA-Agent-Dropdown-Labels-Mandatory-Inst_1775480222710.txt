# REPLIT PROMPT — NLA Agent Dropdown Labels + Mandatory Instructions

Two changes requested by VRS management:
1. Make "Instructions for Technician" a MANDATORY field on all NLA resolutions
2. Update the NLA resolution options to match exact management-approved labels, add a warranty-based replacement split, and convert to a dropdown selector

---

## PART 1 — BACKEND: ADD SECOND REPLACEMENT ACTION

### 1a. server/routes.ts — nlaProcessActionSchema (line 1359)

Add `"nla_replacement_tech_initiates"` to the action enum:

```ts
action: z.enum([
  "nla_replacement_submitted",
  "nla_replacement_tech_initiates",  // NEW — Cinch/SHW variant
  "nla_part_found_vrs_ordered",
  "nla_part_found_tech_orders",
  "nla_escalate_to_pcard",
  "nla_pcard_confirm",
  "nla_reject",
  "nla_invalid",
]),
```

### 1b. server/routes.ts — Make technicianMessage required for approval actions

In the nlaProcessActionSchema, keep `technicianMessage` as optional in the schema (reject/invalid may not need it), but add validation INSIDE the route handler.

After the `const { action, ... } = parsed.data;` line (around line 1402), add this check:

```ts
// Instructions are mandatory for all NLA approval/completion actions
const approvalActions = ["nla_replacement_submitted", "nla_replacement_tech_initiates", "nla_part_found_vrs_ordered", "nla_part_found_tech_orders"];
if (approvalActions.includes(action) && (!technicianMessage || !technicianMessage.trim())) {
  return res.status(400).json({ error: "Instructions for technician are required" });
}
```

Also enforce on reject and invalid — Hamed wants it mandatory for ALL actions:
```ts
if (!technicianMessage || !technicianMessage.trim()) {
  return res.status(400).json({ error: "Instructions for technician are required" });
}
```

### 1c. server/routes.ts — Add the new replacement handler

After the existing `nla_replacement_submitted` handler block (after line 1424), add a new block:

```ts
} else if (action === "nla_replacement_tech_initiates") {
  // Cinch / SHW — tech must initiate replacement in TechHub
  updateData.ticketStatus = "completed";
  updateData.statusChangedAt = new Date();
  updateData.reviewedBy = authReq.user!.id;
  updateData.reviewedAt = new Date();
  updateData.nlaResolution = "replacement_tech_initiates";
  updateData.technicianMessage = technicianMessage || null;

  smsMessage = `VRS NLA Update for SO#${submission.serviceOrder}\n\nStatus: NLA REPLACEMENT APPROVED\nThe part(s) you requested could not be sourced. VRS has approved a replacement.\n\nAction Required: You must initiate the replacement in TechHub. Follow standard replacement procedures in TechHub to process this replacement.`;
  if (technicianMessage) smsMessage += `\n\nInstructions: ${technicianMessage}`;
  smsType = "nla_replacement_tech_initiates";
```

### 1d. Update nlaResolution value in schema comment

In shared/schema.ts, update the comment on the nlaResolution column (line 115):

```ts
nlaResolution: text("nla_resolution"), // 'replacement_submitted', 'replacement_tech_initiates', 'part_found_vrs_ordered', 'part_found_tech_orders'
```

No schema migration needed — it's a text column, any string value works.

---

## PART 2 — AGENT DASHBOARD: CONVERT TO DROPDOWN WITH EXACT LABELS

### 2a. Replace the card-style resolution buttons with a Select dropdown

In agent-dashboard.tsx, find the NLA resolution panel (the section starting around line 1885 with the card buttons under "Part Not Found" and "Part Found" headers).

Replace the ENTIRE card-button section (from the `<div className="space-y-3">` containing "Part Not Found" header through to the end of the "Reject / Close" section, but NOT including the instructions textarea or submit buttons) with a single Select dropdown:

```tsx
<div className="space-y-2">
  <Label className="text-xs font-medium text-muted-foreground uppercase tracking-wider">
    Resolution
  </Label>
  <Select
    value={nlaAction || ""}
    onValueChange={(val) => setNlaAction(val || null)}
  >
    <SelectTrigger data-testid="select-nla-resolution">
      <SelectValue placeholder="Select NLA resolution..." />
    </SelectTrigger>
    <SelectContent>
      <SelectItem value="nla_replacement_submitted">
        Approved Verified NLA VRS Replacement Submitted (SPHW / MPA)
      </SelectItem>
      <SelectItem value="nla_replacement_tech_initiates">
        Approved Verified NLA VRS Replacement Approved (SHW / Cinch — Tech initiates in TechHub)
      </SelectItem>
      <SelectItem value="nla_part_found_vrs_ordered">
        Approve Part Found Locally / Ordered (reschedule the call in TH)
      </SelectItem>
      <SelectItem value="nla_part_found_tech_orders">
        Approve Part Found (Instruct Tech to order part # Provided)
      </SelectItem>
      <SelectItem value="nla_reject">
        Rejected — More Information Needed
      </SelectItem>
      <SelectItem value="nla_invalid">
        Invalid NLA Request
      </SelectItem>
    </SelectContent>
  </Select>
</div>
```

Make sure `Select`, `SelectTrigger`, `SelectValue`, `SelectContent`, and `SelectItem` are imported from `@/components/ui/select` (they should already be imported for other parts of the dashboard).

### 2b. Keep conditional sub-fields below the dropdown

After the dropdown, show conditional fields based on the selected value:

```tsx
{/* Part number input — only when tech needs to order */}
{nlaAction === "nla_part_found_tech_orders" && (
  <div className="space-y-1">
    <Label className="text-xs">Part Number *</Label>
    <Input
      placeholder="Enter part number (e.g., WPW10321304)..."
      value={nlaFoundPartNumber}
      onChange={(e) => setNlaFoundPartNumber(e.target.value.toUpperCase())}
      className="font-mono"
      data-testid="input-nla-part-number"
    />
  </div>
)}

{/* P-card escalation note — only for VRS-orders by non-P-card agent */}
{nlaAction === "nla_part_found_vrs_ordered" && !user?.canOrderParts && (
  <div className="rounded-md border border-amber-200 bg-amber-50 dark:bg-amber-950/20 dark:border-amber-800 p-3">
    <p className="text-xs text-amber-700 dark:text-amber-400 flex items-center gap-1">
      <CreditCard className="w-3 h-3" /> This will be escalated to a P-card agent for ordering
    </p>
  </div>
)}

{/* Rejection reasons — only when rejecting */}
{nlaAction === "nla_reject" && (
  // Keep the existing rejection reason checkboxes exactly as they are now
)}

{/* Invalid reason — only when marking invalid */}
{nlaAction === "nla_invalid" && (
  // Keep the existing invalid reason inputs exactly as they are now
)}
```

### 2c. Make Instructions for Technician mandatory

Change the label from:
```tsx
Instructions for Technician (optional)
```

To:
```tsx
Instructions for Technician *
```

Change the placeholder to:
```tsx
"Provide clear instructions for the technician. This field is required."
```

Add a red asterisk styling on the label consistent with other required fields in the app.

### 2d. Add validation before opening confirmation dialog

In BOTH submit button onClick handlers (the "Escalate to P-Card Agent" button and the "Process & Send NLA Resolution" button), add a check for technicianMessage BEFORE calling `setNlaConfirmOpen(true)`:

```ts
if (!technicianMessage.trim()) {
  toast({ title: "Instructions Required", description: "Please provide instructions for the technician.", variant: "destructive" });
  return;
}
```

Add this right after the existing validation checks (after the nlaAction check, after the part number check, after the rejection reasons check).

### 2e. Disable submit button when instructions empty

Add `|| !technicianMessage.trim()` to the `disabled` condition on BOTH submit buttons:

```tsx
disabled={!nlaAction || nlaProcessMutation.isPending || !technicianMessage.trim()}
```

This grays out the button until the agent types instructions, giving immediate visual feedback that the field is required.

---

## PART 3 — UPDATE CONFIRMATION DIALOG LABELS

### 3a. Update the confirmation dialog descriptions (around line 2990)

Update the dialog description text to use the new labels:

```tsx
{nlaAction === "nla_replacement_submitted" ? (
  <>Resolution: Approved Verified NLA VRS Replacement Submitted. The technician will be notified to close the call using the NLA labor code.</>
) : nlaAction === "nla_replacement_tech_initiates" ? (
  <>Resolution: Approved Verified NLA VRS Replacement Approved. The technician will be notified to initiate the replacement in TechHub.</>
) : nlaAction === "nla_part_found_vrs_ordered" ? (
  <>Resolution: Part Found Locally / Ordered. The technician will be notified that VRS has ordered the part.</>
) : nlaAction === "nla_part_found_tech_orders" ? (
  <>Resolution: Part Found — Tech to Order. The technician will be notified to order part <strong className="font-mono">{nlaFoundPartNumber}</strong> from TechHub.</>
) : nlaAction === "nla_reject" ? (
  <>The technician will be notified that more information is needed and can resubmit.</>
) : nlaAction === "nla_invalid" ? (
  <>The technician will be notified that this NLA request is invalid.</>
) : null}
```

### 3b. Add the new action to the confirmation mutation handler

In the mutation call inside the AlertDialogAction onClick (around line 3050), the existing `nlaProcessMutation.mutate(body)` already passes `action: nlaAction` which will include the new `nla_replacement_tech_initiates` value. No change needed here — it's already generic.

---

## PART 4 — UPDATE TECHNICIAN DETAIL VIEW

### 4a. submission-detail.tsx — Add the new replacement resolution display

Find where NLA resolutions are displayed (around line 233). After the existing `replacement_submitted` case, add:

```tsx
{sub.nlaResolution === "replacement_tech_initiates" && (
  <div>
    <p className="font-semibold text-emerald-700 dark:text-emerald-400">NLA Replacement Approved</p>
    <p className="text-sm text-muted-foreground mt-1">
      The part(s) could not be sourced. VRS has approved a replacement. You must initiate the replacement in TechHub using standard replacement procedures.
    </p>
  </div>
)}
```

---

## PART 5 — UPDATE ESCALATED TICKET DISPLAY

### 5a. Escalated ticket banner in agent dashboard

The escalated ticket view (around line 1814) shows the resolution label. Add the new resolution to the label mapping:

Where it currently maps resolution labels, add:
```tsx
selectedSubmission.nlaResolution === "replacement_tech_initiates" ? "Replacement Approved — Tech Initiates" :
```

---

## PART 6 — UPDATE EXPORTS

### 6a. Add new resolution label to export mapping

In both CSV and XLSX export routes, wherever resolution values are mapped to display labels, add:
- `replacement_tech_initiates` → "Replacement Approved (Tech Initiates)"

---

## IMPORTANT NOTES

- The `Select` component from shadcn/ui should already be imported in agent-dashboard.tsx. If not, add the import.
- Do NOT modify the existing `/api/submissions/:id/process` route — only the `/process-nla` route.
- The instructions field is mandatory for ALL NLA actions including reject and invalid. Hamed's request: "can you make instruction for technician mandatory field for agent to type and explain as we get a mixed of new and tenured techs."
- The dropdown labels are intentionally verbose — they match exactly what management specified. Do NOT shorten them.
- The P-card escalation logic remains the same. The dropdown just changes the UI; the escalation button still appears when a non-P-card agent selects "Part Found Locally / Ordered."
- Preserve all existing functionality.
- No schema migration needed — nlaResolution is a text column.
- Update replit.md to document the NLA resolution dropdown labels and mandatory instructions requirement.