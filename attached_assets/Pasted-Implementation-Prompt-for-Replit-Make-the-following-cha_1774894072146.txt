Implementation Prompt for Replit

Make the following changes across four files. Do not modify anything outside what is specified.

1. shared/schema.ts — Add two columns to the submissions table
Inside the pgTable("submissions", { ... }) definition, add these two fields before the createdAt field:
tsprocId: varchar("proc_id", { length: 50 }),
clientNm: varchar("client_nm", { length: 150 }),
Then run a database migration to add these columns.

2. server/services/snowflake.ts — Add a new export function
Add a new exported interface and function at the bottom of the file:
tsexport interface ProcIdResult {
  procId: string;
  clientNm: string;
}

export async function fetchProcIdForServiceOrder(rawServiceOrder: string): Promise<ProcIdResult> {
  const soNumber = rawServiceOrder.includes("-")
    ? rawServiceOrder.split("-")[1]
    : rawServiceOrder;

  try {
    const connection = await getConnection();
    try {
      const rows: any[] = await new Promise((resolve, reject) => {
        connection.execute({
          sqlText: `SELECT CMB_THD_PTY_ID, CLIENT_NM FROM PRD_DB2.HS_PERM_TBLS.IH_SERVICEORDER_FULLDETAIL_TBL WHERE SO_NO = ? LIMIT 1`,
          binds: [soNumber],
          complete: (err: any, stmt: any, rows: any) => {
            if (err) reject(err);
            else resolve(rows || []);
          },
        });
      });

      if (rows.length > 0) {
        return {
          procId: (rows[0].CMB_THD_PTY_ID || "").trim() || "Not Found",
          clientNm: (rows[0].CLIENT_NM || "").trim() || "Not Found",
        };
      }
      return { procId: "Not Found", clientNm: "Not Found" };
    } finally {
      connection.destroy((err: any) => {
        if (err) console.error("Snowflake disconnect error:", err.message);
      });
    }
  } catch (err) {
    console.error("fetchProcIdForServiceOrder error:", err);
    return { procId: "Not Found", clientNm: "Not Found" };
  }
}

3. server/routes.ts — Call the lookup before createSubmission
At the top of the file (or wherever fetchTechniciansFromSnowflake is imported), add fetchProcIdForServiceOrder to the import:
tsimport { fetchTechniciansFromSnowflake, fetchProcIdForServiceOrder } from "./services/snowflake";
Inside the POST /api/submissions route, immediately before the storage.createSubmission({...}) call, add:
tsconst procIdResult = await fetchProcIdForServiceOrder(parsed.data.serviceOrder);
Then inside the createSubmission({...}) call, add these two fields:
tsprocId: procIdResult.procId,
clientNm: procIdResult.clientNm,

4. client/src/pages/agent-dashboard.tsx — Display in Technician Info card
In the Technician Info card (around line 1236), which already shows Name, RAC ID, Phone, District in a 2-column grid — add two new cells to that grid after the existing four:
tsx<div>
  <p className="text-xs text-muted-foreground">ProcID</p>
  <p className="text-sm font-medium">{selectedTicket.procId || "Not Found"}</p>
</div>
<div>
  <p className="text-xs text-muted-foreground">Client</p>
  <p className="text-sm font-medium">{selectedTicket.clientNm || "Not Found"}</p>
</div>

5. client/src/pages/admin-dashboard.tsx — Display in Details tab
In the Details tab grid (around line 486), which already shows Technician, Status, Assigned Agent, Division, Warranty, Request Type — add two new cells to that grid:
tsx<div>
  <p className="text-xs text-muted-foreground">ProcID</p>
  <p className="text-sm font-medium">{ticket.procId || "Not Found"}</p>
</div>
<div>
  <p className="text-xs text-muted-foreground">Client</p>
  <p className="text-sm font-medium">{ticket.clientNm || "Not Found"}</p>
</div>