# VRS Diagnostic Prompt — Answer These Questions (Do NOT Make Any Code Changes)

**IMPORTANT: This is an information-gathering prompt only. Do NOT modify any code, schema, or files. Just read the codebase and answer each question below with specifics.**

---

## 1. Snowflake Sync Query
Open `server/services/snowflake.ts` and paste the EXACT current SQL query used to sync technicians. Include all selected columns and WHERE/filter clauses.

## 2. Technician Table Schema
Open `shared/schema.ts` and paste the full `technicians` table definition (the Drizzle schema). List every column, its type, and whether it's nullable.

## 3. Users Table — Technician Records
When a technician logs in for the first time via `getOrCreateTechUser()` in `server/routes.ts`:
- What exact fields are written to the `users` table?
- Is there any field that distinguishes "has logged in at least once" vs "never logged in"?
- Paste the relevant `getOrCreateTechUser()` function code.

## 4. Submissions Table — Technician Linkage
In `shared/schema.ts`, what field on the `submissions` table links a ticket to a technician? Is it `technicianLdapId`? Confirm the exact column name and type.

## 5. Current Users Tab Query
In `server/routes.ts`, find the API endpoint that returns the user list for the Admin dashboard Users tab (likely `GET /api/admin/users` or similar). Paste:
- The route path
- The database query or storage method it calls
- Whether it currently filters by role or returns all users including technicians

## 6. Current Users Tab Frontend
In `client/src/pages/admin-dashboard.tsx`, find the section that renders the Users tab / User Management table. Does it currently have any tab structure, or is it a single flat table? If there are already tabs or sections in the admin dashboard sidebar, list them.

## 7. WebSocket Notification Sound
In `client/src/lib/websocket.ts`, find the notification ding/tone code. Paste the exact code block that generates the audio tone — specifically the frequency, duration, gain/volume values, and any oscillator settings.

## 8. Snowflake Available Columns
From the Snowflake sync query in question 1, are any of the following columns currently being pulled OR available in the `COMTTU_TECH_UN` table:
- A job title or role/position field (e.g., `TECH_TITLE`, `JOB_TITLE`, `POSITION`)
- District name or code (confirm which column and what's currently being stored)
- Any other columns in the SELECT that aren't currently being saved to the local `technicians` table

---

**Output format:** Answer each numbered question with the actual code snippets and specifics requested. Do not summarize or paraphrase — paste the real code.