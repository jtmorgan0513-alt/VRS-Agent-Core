## Goal

American Home Shield (AHS) and First American warranty submissions need to be enabled in VRS. The backend two-stage approval logic, agent dashboard UI, and admin dashboard display for these providers are ALREADY FULLY BUILT. This change ONLY removes the “Coming Soon” gates on the technician submission side and updates the related Zod schemas to accept the new warranty types. DO NOT modify any agent dashboard, admin dashboard, WebSocket, or backend approval/claim logic — it is already correct.

## Changes Required

### 1. `client/src/pages/tech-submit.tsx`

**a) WARRANTY_PROVIDERS array (~line 44–48):**
Change `available: false` to `available: true` for both `american_home_shield` and `first_american`:

```ts
const WARRANTY_PROVIDERS = [
  { value: "sears_protect", label: "Sears Protect / Sears PA / Sears Home Warranty (Cinch)", available: true },
  { value: "american_home_shield", label: "American Home Shield", available: true },
  { value: "first_american", label: "First American", available: true },
];
```

**b) Zod schema `warrantyType` enum (~line 57):**
Expand from `z.enum(["sears_protect"])` to:

```ts
warrantyType: z.enum(["sears_protect", "american_home_shield", "first_american"]).default("sears_protect"),
```

**c) Provider selection onClick handler (~line 749):**
Currently the onClick always hardcodes `form.setValue("warrantyType", "sears_protect")`. Change it to set the actual selected provider’s value AND set `warrantyProvider` to the provider’s label:

```ts
onClick={() => {
  if (provider.available) {
    form.setValue("warrantyType", provider.value as any);
    form.setValue("warrantyProvider", provider.label);
  }
}}
```

**d) Selection highlight (~line 743):**
Currently the highlight condition is hardcoded to check `form.watch("warrantyType") === "sears_protect" && provider.value === "sears_protect"`. Change to dynamically highlight whichever provider is selected:

```ts
form.watch("warrantyType") === provider.value
  ? "border-primary bg-primary/5"
  : ""
```

**e) Default warrantyProvider value (~line 488):**
Change the default from empty string to the Sears Protect label since it’s the default warrantyType:

```ts
warrantyProvider: "Sears Protect / Sears PA / Sears Home Warranty (Cinch)",
```

**f) Tooltip text (~line 732):**
Change from `"Select Sears Protect. B2B providers coming soon."` to:

```ts
"Select the warranty provider for this service order."
```

### 2. `server/routes.ts`

**a) `createSubmissionSchema` warrantyType enum (~line 476):**
Expand from `z.enum(["sears_protect"])` to:

```ts
warrantyType: z.enum(["sears_protect", "american_home_shield", "first_american"]).default("sears_protect"),
```

This is the ONLY change in routes.ts. Do NOT touch any other logic in this file.

### 3. `client/src/pages/help-center.tsx`

**a) FAQ entry (~line 91–93):**
Change the title from `"What does the warranty provider \"Coming Soon\" mean?"` to:

```ts
"Which warranty providers are supported?"
```

Change the content from the current text to:

```ts
"VRS Submit currently supports Sears Protect (Cinch), American Home Shield (AHS), and First American warranty submissions. Select the correct warranty provider during the submission process. Additional providers may be added in the future."
```

## Critical Preservation Rules

- DO NOT modify anything in `agent-dashboard.tsx` — the two-stage review flow, `isTwoStageWarranty()`, `needsExternalAuth()`, `getProviderAuthLabel()`, external auth code input, and approval validation are all already correctly built for AHS and First American.
- DO NOT modify anything in `admin-dashboard.tsx` — AHS and First American display labels are already handled.
- DO NOT modify `server/websocket.ts` — WARRANTY_LABELS already includes `ahs` and `first_american` entries.
- DO NOT modify the claim, approval, rejection, or SMS logic in `server/routes.ts` — the two-stage SMS messaging and external auth code enforcement are already correct.
- DO NOT modify `shared/schema.ts` — the `warrantyType` column is already a plain text field with no enum constraint.
- DO NOT add any new database columns, API endpoints, or WebSocket events.
- Preserve ALL existing functionality for Sears Protect submissions — they must continue to work exactly as they do today.