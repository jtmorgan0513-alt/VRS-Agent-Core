# REPLIT PROMPT — NLA Resolution Actions + P-Card Escalation + Admin Timing Fix

Three changes in this prompt:
1. Replace the standard Approve/Reject/Invalid action panel with NLA-specific resolution options for NLA tickets
2. Add P-card flag to users and build the escalation workflow for non-P-card agents
3. Fix the admin ticket overview timing columns to show meaningful operational metrics

---

## PART 1 — SCHEMA CHANGES

### 1a. shared/schema.ts — users table: add canOrderParts

Add after the `agentStatus` column (line 26):

```ts
canOrderParts: boolean("can_order_parts").notNull().default(false),
```

This is the P-card flag. Only agents with this set to `true` can finalize NLA orders where VRS is purchasing the part.

### 1b. shared/schema.ts — submissions table: add claimedAt

Add after `assignedTo` (around line 112):

```ts
claimedAt: timestamp("claimed_at"),
```

Records when an agent first claims a ticket. Never overwritten — permanently captures the moment the ticket left the queue.

### 1c. shared/schema.ts — submissions table: add NLA resolution columns

Add after `claimedAt`:

```ts
nlaResolution: text("nla_resolution"), // 'replacement_submitted', 'part_found_vrs_ordered', 'part_found_tech_orders'
nlaFoundPartNumber: varchar("nla_found_part_number", { length: 100 }),
nlaEscalatedBy: integer("nla_escalated_by").references(() => users.id),
```

- `nlaResolution` — which resolution the agent selected
- `nlaFoundPartNumber` — part number the agent found (when tech needs to order)
- `nlaEscalatedBy` — the non-P-card agent who researched the ticket before escalating to a P-card agent

Run `db:push` after all schema changes.

---

## PART 2 — P-CARD TOGGLE IN ADMIN USER MANAGEMENT

### 2a. admin-dashboard.tsx — Add P-card toggle to user edit/detail

In the user management section where admins edit agent details (near the division assignment area), add a toggle/checkbox:

**Label:** "Can Order Parts (P-Card)"
**Description text below toggle:** "Enable for agents who have a P-card and can place parts orders."

This should only be visible when editing a user with role `vrs_agent` or `admin`. It's a simple boolean toggle that updates the `canOrderParts` field.

### 2b. admin-dashboard.tsx — Show P-card badge in user list

In the user list table, add a small badge or icon next to agents who have `canOrderParts === true`. A small credit-card icon or "P-Card" badge in the row. This gives admins a quick visual of who can order parts.

### 2c. server/routes.ts — Allow updating canOrderParts

In the `updateUserSchema` (the Zod schema for PATCH `/api/admin/users/:id`), add:

```ts
canOrderParts: z.boolean().optional(),
```

And in the update handler, pass it through to `storage.updateUser()`:

```ts
if (parsed.data.canOrderParts !== undefined) {
  updateData.canOrderParts = parsed.data.canOrderParts;
}
```

### 2d. Expose canOrderParts to agent dashboard

The agent dashboard needs to know if the current user has a P-card. The `/api/auth/me` response already returns the full user object. Make sure `canOrderParts` is included in the sanitized user response (it should be automatic since `sanitizeUser` only strips `password`). The agent dashboard can then check `user.canOrderParts` to determine which action buttons to show.

---

## PART 3 — BACKEND: SET claimedAt ON TICKET CLAIM

### 3a. server/routes.ts — Ticket claim route (around line 1053)

In the `updateSubmission` call inside the claim route, add `claimedAt`:

```ts
const updated = await storage.updateSubmission(id, {
  ticketStatus: "pending",
  statusChangedAt: new Date(),
  claimedAt: new Date(),
  assignedTo: authReq.user!.id,
  updatedAt: new Date(),
} as any);
```

### 3b. Resubmission auto-assignment

When a resubmission is auto-assigned to the original agent (around line 600–610), also set `claimedAt: new Date()` in the submission creation data.

---

## PART 4 — NLA RESOLUTION: BACKEND API

### 4a. server/routes.ts — NLA process action schema

Add near the existing `processActionSchema`:

```ts
const nlaProcessActionSchema = z.object({
  action: z.enum([
    "nla_replacement_submitted",
    "nla_part_found_vrs_ordered",
    "nla_part_found_tech_orders",
    "nla_escalate_to_pcard",
    "nla_pcard_confirm",
    "nla_reject",
    "nla_invalid",
  ]),
  agentNotes: z.string().optional(),
  technicianMessage: z.string().optional(),
  nlaFoundPartNumber: z.string().optional(),
  nlaResolution: z.string().optional(),
  rejectionReasons: z.array(z.string()).optional(),
  invalidReason: z.string().optional(),
  invalidInstructions: z.string().optional(),
});
```

### 4b. server/routes.ts — NLA process route

Add a NEW route for NLA ticket processing. Do NOT modify the existing `/api/submissions/:id/process` route.

```ts
app.patch("/api/submissions/:id/process-nla", authenticateToken, requireRole("vrs_agent", "admin"), async (req, res) => {
  try {
    const authReq = req as AuthenticatedRequest;
    const id = parseInt(req.params.id as string);
    if (isNaN(id)) return res.status(400).json({ error: "Invalid submission ID" });

    const parsed = nlaProcessActionSchema.safeParse(req.body);
    if (!parsed.success) return res.status(400).json({ error: parsed.error.errors[0].message });

    const submission = await storage.getSubmission(id);
    if (!submission) return res.status(404).json({ error: "Submission not found" });

    if (submission.requestType !== "parts_nla") {
      return res.status(400).json({ error: "This route is only for NLA tickets" });
    }

    if (submission.ticketStatus !== "pending") {
      return res.status(400).json({ error: "Ticket must be in pending status" });
    }

    if (submission.assignedTo !== authReq.user!.id && authReq.user!.role !== "admin" && authReq.user!.role !== "super_admin") {
      return res.status(403).json({ error: "This ticket is not assigned to you" });
    }

    const { action, agentNotes, technicianMessage, nlaFoundPartNumber, nlaResolution, rejectionReasons, invalidReason, invalidInstructions } = parsed.data;
    const currentUser = await storage.getUser(authReq.user!.id);

    const updateData: Record<string, unknown> = {
      agentNotes: agentNotes || null,
      updatedAt: new Date(),
    };

    let smsMessage: string = "";
    let smsType: string = "";
    let shouldSendSms = true;

    // ── REPLACEMENT SUBMITTED ──────────────────────────────
    if (action === "nla_replacement_submitted") {
      updateData.ticketStatus = "completed";
      updateData.statusChangedAt = new Date();
      updateData.reviewedBy = authReq.user!.id;
      updateData.reviewedAt = new Date();
      updateData.nlaResolution = "replacement_submitted";
      updateData.technicianMessage = technicianMessage || null;

      smsMessage = `VRS NLA Update for SO#${submission.serviceOrder}\n\nStatus: REPLACEMENT SUBMITTED\nThe part(s) you requested could not be sourced. A replacement request has been submitted to the warranty company.\n\nAction Required: Close the call using the NLA labor code.`;
      if (technicianMessage) smsMessage += `\n\nInstructions: ${technicianMessage}`;
      smsType = "nla_replacement_submitted";

    // ── PART FOUND — VRS ORDERS (P-card agent completing) ──
    } else if (action === "nla_part_found_vrs_ordered") {
      if (!currentUser?.canOrderParts) {
        return res.status(403).json({ error: "You do not have a P-card. Use 'Escalate to P-Card Agent' instead." });
      }
      updateData.ticketStatus = "completed";
      updateData.statusChangedAt = new Date();
      updateData.reviewedBy = authReq.user!.id;
      updateData.reviewedAt = new Date();
      updateData.nlaResolution = "part_found_vrs_ordered";
      updateData.technicianMessage = technicianMessage || null;

      smsMessage = `VRS NLA Update for SO#${submission.serviceOrder}\n\nStatus: PART FOUND — ORDERED BY VRS\nThe VRS parts team has located and ordered the part(s) for this service order.`;
      if (technicianMessage) smsMessage += `\n\nInstructions: ${technicianMessage}`;
      smsType = "nla_part_ordered_vrs";

    // ── PART FOUND — TECH ORDERS ───────────────────────────
    } else if (action === "nla_part_found_tech_orders") {
      if (!nlaFoundPartNumber || !nlaFoundPartNumber.trim()) {
        return res.status(400).json({ error: "Part number is required when the technician needs to order" });
      }
      updateData.ticketStatus = "completed";
      updateData.statusChangedAt = new Date();
      updateData.reviewedBy = authReq.user!.id;
      updateData.reviewedAt = new Date();
      updateData.nlaResolution = "part_found_tech_orders";
      updateData.nlaFoundPartNumber = nlaFoundPartNumber.trim().toUpperCase();
      updateData.technicianMessage = technicianMessage || null;

      smsMessage = `VRS NLA Update for SO#${submission.serviceOrder}\n\nStatus: PART FOUND — YOU NEED TO ORDER\nPart Number: ${nlaFoundPartNumber.trim().toUpperCase()}\n\nThis part is available in TechHub. Order it and reschedule the call.`;
      if (technicianMessage) smsMessage += `\n\nInstructions: ${technicianMessage}`;
      smsType = "nla_part_tech_orders";

    // ── ESCALATE TO P-CARD AGENT ───────────────────────────
    } else if (action === "nla_escalate_to_pcard") {
      // Non-P-card agent has done the research. Save their work and release to NLA queue.
      // Only P-card agents should pick this up next.
      if (!nlaResolution) {
        return res.status(400).json({ error: "Resolution type is required for escalation" });
      }
      updateData.nlaResolution = nlaResolution; // e.g., "part_found_vrs_ordered"
      updateData.nlaFoundPartNumber = nlaFoundPartNumber?.trim().toUpperCase() || null;
      updateData.nlaEscalatedBy = authReq.user!.id;
      updateData.technicianMessage = technicianMessage || null;
      updateData.reassignmentNotes = `Researched by ${currentUser?.name || "agent"}. Ready for P-card order.`;
      updateData.assignedTo = null;
      updateData.ticketStatus = "queued";
      updateData.statusChangedAt = new Date();

      // Do NOT send SMS to tech — they don't need to know about internal escalation
      shouldSendSms = false;

      // Broadcast back to NLA queue — only P-card agents should pick this up
      broadcastToDivisionAgents("nla", {
        type: "nla_escalated",
        payload: {
          submissionId: submission.id,
          serviceOrder: submission.serviceOrder,
          escalatedBy: currentUser?.name || "Agent",
          resolution: nlaResolution,
        },
      });

    // ── P-CARD AGENT CONFIRMS ESCALATED TICKET ─────────────
    } else if (action === "nla_pcard_confirm") {
      if (!currentUser?.canOrderParts) {
        return res.status(403).json({ error: "Only P-card agents can confirm escalated orders" });
      }
      // The resolution and part info were pre-filled by the escalating agent
      const resolution = submission.nlaResolution || nlaResolution;
      updateData.ticketStatus = "completed";
      updateData.statusChangedAt = new Date();
      updateData.reviewedBy = authReq.user!.id;
      updateData.reviewedAt = new Date();
      updateData.nlaResolution = resolution;
      if (technicianMessage) updateData.technicianMessage = technicianMessage;

      if (resolution === "part_found_vrs_ordered") {
        smsMessage = `VRS NLA Update for SO#${submission.serviceOrder}\n\nStatus: PART FOUND — ORDERED BY VRS\nThe VRS parts team has located and ordered the part(s) for this service order.`;
      } else if (resolution === "part_found_tech_orders") {
        const partNum = submission.nlaFoundPartNumber || nlaFoundPartNumber || "";
        smsMessage = `VRS NLA Update for SO#${submission.serviceOrder}\n\nStatus: PART FOUND — YOU NEED TO ORDER\nPart Number: ${partNum}\n\nThis part is available in TechHub. Order it and reschedule the call.`;
      } else {
        smsMessage = `VRS NLA Update for SO#${submission.serviceOrder}\n\nYour NLA parts request has been processed by the VRS team.`;
      }
      if (technicianMessage || submission.technicianMessage) {
        smsMessage += `\n\nInstructions: ${technicianMessage || submission.technicianMessage}`;
      }
      smsType = "nla_pcard_confirmed";

    // ── REJECT ─────────────────────────────────────────────
    } else if (action === "nla_reject") {
      updateData.ticketStatus = "rejected";
      updateData.statusChangedAt = new Date();
      updateData.reviewedBy = authReq.user!.id;
      updateData.reviewedAt = new Date();
      updateData.assignedTo = null;
      updateData.rejectionReasons = rejectionReasons ? JSON.stringify(rejectionReasons) : null;
      updateData.technicianMessage = technicianMessage || null;

      const host = req.get("host") || "";
      const protocol = req.get("x-forwarded-proto") || req.protocol || "https";
      const resubmitLink = `${protocol}://${host}/tech/resubmit/${submission.id}`;
      const reasonText = rejectionReasons?.join(", ") || "More information needed";

      smsMessage = `VRS NLA Update for SO#${submission.serviceOrder}\n\nStatus: MORE INFO NEEDED\nReason: ${reasonText}\n\nTap to resubmit:\n${resubmitLink}`;
      if (technicianMessage) smsMessage += `\n\nAgent notes: ${technicianMessage}`;
      smsType = "nla_rejected";

      broadcastToDivisionAgents("nla", {
        type: "ticket_released",
        payload: { submissionId: submission.id },
      });

    // ── INVALID ────────────────────────────────────────────
    } else if (action === "nla_invalid") {
      if (!invalidReason) return res.status(400).json({ error: "Invalid reason is required" });
      updateData.ticketStatus = "invalid";
      updateData.statusChangedAt = new Date();
      updateData.reviewedBy = authReq.user!.id;
      updateData.reviewedAt = new Date();
      updateData.invalidReason = invalidReason;
      updateData.invalidInstructions = invalidInstructions || null;

      smsMessage = `VRS NLA Update for SO#${submission.serviceOrder}\n\nStatus: INVALID NLA REQUEST\nReason: ${invalidReason}`;
      if (invalidInstructions) smsMessage += `\n\nInstructions: ${invalidInstructions}`;
      smsType = "nla_invalid";

    } else {
      return res.status(400).json({ error: "Invalid action" });
    }

    const updated = await storage.updateSubmission(id, updateData as any);

    // Send SMS (skip for internal escalations)
    if (shouldSendSms && smsMessage) {
      const smsPhone = submission.phoneOverride || submission.phone;
      await sendSms(submission.id, smsPhone, smsType, smsMessage);
    }

    // Update agent status back to online
    if (authReq.user!.role === "vrs_agent") {
      await storage.updateUser(authReq.user!.id, { agentStatus: "online", updatedAt: new Date() } as any);
      updateClientStatus(authReq.user!.id, "online");
    }

    // Broadcast updates
    broadcastToAdmins({
      type: "ticket_updated",
      payload: { submissionId: id, ticketStatus: updated?.ticketStatus },
    });
    await broadcastVrsAvailability();

    return res.status(200).json({ submission: updated });
  } catch (error) {
    console.error("NLA process error:", error);
    return res.status(500).json({ error: "Failed to process NLA ticket" });
  }
});
```

Make sure `broadcastVrsAvailability`, `broadcastToDivisionAgents`, `broadcastToAdmins`, `updateClientStatus`, and `sendSms` are accessible in this scope.

---

## PART 5 — NLA RESOLUTION: AGENT DASHBOARD UI

### 5a. New state variables

Add near existing action state:
```ts
const [nlaAction, setNlaAction] = useState<string | null>(null);
const [nlaFoundPartNumber, setNlaFoundPartNumber] = useState("");
```

Reset both when `selectedId` changes.

### 5b. Detect escalated tickets

An escalated ticket has `nlaEscalatedBy` set and `ticketStatus === "queued"` (it was sent back to the queue by a non-P-card agent). When a P-card agent claims it, they should see a banner indicating it was pre-researched.

### 5c. NLA Action Panel — conditional on requestType

When `selectedSubmission.requestType === "parts_nla"`, render the NLA action panel INSTEAD of the standard approve/reject/invalid panel. The NLA panel has two modes:

**MODE A — Fresh NLA ticket (or non-escalated)**
Show all resolution options. The behavior of "Part Found — VRS Orders" depends on whether the current user has `canOrderParts`:

```
┌─────────────────────────────────────────────────────────┐
│ NLA Parts Resolution                                    │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ ── PART NOT FOUND ────────────────────────────────────  │
│ ○ Replacement Submitted to Warranty                     │
│   Part not available. Replacement sent to warranty.     │
│   Tech closes call with NLA labor code.                 │
│                                                         │
│ ── PART FOUND ────────────────────────────────────────  │
│ ○ Part Ordered by VRS                                   │
│   VRS located and will order the part.                  │
│   [if non-P-card: shows note "Will escalate to          │
│    P-card agent for ordering"]                          │
│                                                         │
│ ○ Tech to Order — Part # Provided                       │
│   Part available in TechHub. Tech orders it.            │
│   ┌─────────────────────────────────────┐               │
│   │ Part Number: [________________]     │               │
│   └─────────────────────────────────────┘               │
│                                                         │
│ ── REJECT / CLOSE ────────────────────────────────────  │
│ ○ Reject — Need More Info                               │
│   (shows rejection reason checkboxes)                   │
│                                                         │
│ ○ Invalid NLA Request                                   │
│   (shows invalid reason + instructions inputs)          │
│                                                         │
│ ┌──────────────────────────────────────────────────┐    │
│ │ Instructions for Technician (optional)            │    │
│ │ [                                                ]│    │
│ └──────────────────────────────────────────────────┘    │
│                                                         │
│ [Process & Send] or [Escalate to P-Card Agent]          │
└─────────────────────────────────────────────────────────┘
```

**Button logic:**
- If the agent has `canOrderParts === true` OR if the selected resolution does NOT require a P-card (`replacement_submitted`, `part_found_tech_orders`, `nla_reject`, `nla_invalid`): show **"Process & Send NLA Resolution"** button. Calls `/api/submissions/:id/process-nla` with the appropriate action.
- If the agent has `canOrderParts === false` AND the selected resolution is `nla_part_found_vrs_ordered`: show **"Escalate to P-Card Agent"** button instead. Calls `/api/submissions/:id/process-nla` with `action: "nla_escalate_to_pcard"` and `nlaResolution: "part_found_vrs_ordered"`.

**MODE B — Escalated ticket (P-card agent reviewing)**
When a P-card agent opens an NLA ticket that has `nlaEscalatedBy` set, show a different view:

```
┌─────────────────────────────────────────────────────────┐
│ NLA Parts — Escalated for P-Card Order                  │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ ┌─ PRE-FILLED BY RESEARCH AGENT ─────────────────────┐ │
│ │ Researched by: [Agent Name]                         │ │
│ │ Resolution: Part Found — VRS Orders                 │ │
│ │ Part Number: WPW10321304 (if provided)              │ │
│ │ Instructions: "Order from XYZ vendor..."            │ │
│ └─────────────────────────────────────────────────────┘ │
│                                                         │
│ ┌──────────────────────────────────────────────────┐    │
│ │ Additional Instructions (optional)               │    │
│ │ [                                                ]│    │
│ └──────────────────────────────────────────────────┘    │
│                                                         │
│              [Confirm & Complete Order]                  │
└─────────────────────────────────────────────────────────┘
```

The "Confirm & Complete Order" button calls `/api/submissions/:id/process-nla` with `action: "nla_pcard_confirm"`. This finalizes the ticket and sends the SMS to the technician.

The P-card agent can also edit the instructions before confirming — their additional notes are appended to the original agent's instructions.

### 5d. Escalated ticket visual indicator in NLA queue

In the NLA queue ticket list, escalated tickets (where `nlaEscalatedBy` is set) should show a distinct visual indicator:

- A small badge: **"Ready for Order"** in green
- Or a credit card icon to indicate P-card action needed

Non-P-card agents should still SEE escalated tickets in the queue but should NOT be able to claim them. When a non-P-card agent tries to claim an escalated ticket, the backend should return an error. Add this check to the claim route:

```ts
// In the claim route, after checking division access:
if (submission.nlaEscalatedBy && !currentUser?.canOrderParts) {
  return res.status(403).json({ error: "This ticket requires a P-card holder. It has been pre-researched and is ready for parts ordering." });
}
```

Alternatively, hide escalated tickets from non-P-card agents entirely in the NLA queue. Either approach works — hiding is simpler UX.

### 5e. Confirmation popup

Same pattern as existing process confirmation:
```
Resolution: [selected option label]
Part Number: [if applicable]
Instructions: [if entered]

This will send an SMS to the technician.

[Cancel]  [Confirm & Send]
```

For escalation:
```
You are escalating this ticket to a P-Card agent for ordering.

Resolution: Part Found — VRS Orders
Your research notes will be saved. The P-Card agent will review and complete the order.

[Cancel]  [Escalate]
```

### 5f. NLA process mutation

```ts
const nlaProcessMutation = useMutation({
  mutationFn: async (data: any) => {
    const res = await apiRequest("PATCH", `/api/submissions/${selectedId}/process-nla`, data);
    return await res.json();
  },
  onSuccess: () => {
    queryClient.invalidateQueries({ queryKey: ["/api/submissions"] });
    queryClient.invalidateQueries({ queryKey: ["/api/agent/stats"] });
    setSelectedId(null);
    setNlaAction(null);
    setNlaFoundPartNumber("");
    toast({ title: "NLA Ticket Processed", description: "Resolution sent." });
  },
  onError: (error: Error) => {
    toast({ title: "Processing Failed", description: error.message, variant: "destructive" });
  },
});
```

---

## PART 6 — ADMIN TICKET OVERVIEW: TIMING COLUMNS FIX

### 6a. Replace three timing column headers (around line 1008)

Replace:
```tsx
<TableHead className="text-right">Age</TableHead>
<TableHead className="text-right">Time in Status</TableHead>
<TableHead className="text-right">Processing Time</TableHead>
```

With:
```tsx
<TableHead className="text-right">Queue Wait</TableHead>
<TableHead className="text-right">Handle Time</TableHead>
<TableHead className="text-right">Total Time</TableHead>
```

### 6b. Replace the three timing cell computations (around line 1072–1080)

Replace with:

```tsx
{/* Queue Wait: createdAt → claimedAt */}
<TableCell className="text-sm font-medium text-right whitespace-nowrap">
  {ticket.claimedAt
    ? getTimeInStatus(ticket.createdAt, ticket.claimedAt)
    : ticket.ticketStatus === "queued"
    ? <span className="text-amber-600 dark:text-amber-400">{getTimeInStatus(ticket.createdAt)} ⏳</span>
    : getTimeInStatus(ticket.createdAt, ticket.statusChangedAt)
  }
</TableCell>

{/* Handle Time: claimedAt → statusChangedAt */}
<TableCell className="text-sm text-right whitespace-nowrap">
  {ticket.claimedAt
    ? ticket.ticketStatus === "pending"
      ? <span className="text-blue-600 dark:text-blue-400">{getTimeInStatus(ticket.claimedAt)} ⏳</span>
      : getTimeInStatus(ticket.claimedAt, ticket.statusChangedAt)
    : "—"
  }
</TableCell>

{/* Total Time: createdAt → statusChangedAt */}
<TableCell className="text-sm text-right whitespace-nowrap">
  {["completed", "approved", "rejected", "rejected_closed", "invalid"].includes(ticket.ticketStatus)
    ? getTimeInStatus(ticket.createdAt, ticket.statusChangedAt)
    : <span className="text-muted-foreground">{getTimeInStatus(ticket.createdAt)}</span>
  }
</TableCell>
```

### 6c. Add claimedAt and NLA columns to exports

In both CSV and XLSX export routes, add columns:
- "Claimed At" — `ticket.claimedAt ? new Date(ticket.claimedAt).toISOString() : ""`
- On the NLA XLSX sheet, also add: "NLA Resolution" and "Found Part Number"

Map resolution values to labels:
- `replacement_submitted` → "Replacement Submitted"
- `part_found_vrs_ordered` → "Part Ordered by VRS"
- `part_found_tech_orders` → "Tech Orders Part"

---

## PART 7 — TECHNICIAN DETAIL VIEW: NLA RESOLUTION DISPLAY

When a tech views a completed NLA ticket on their submission detail page:

- **Replacement Submitted**: "A replacement request has been submitted to the warranty company. Close this call using the NLA labor code."
- **Part Ordered by VRS**: "VRS has ordered the part for this service order. You will be contacted with further details."
- **Tech Orders Part**: Show the part number prominently: "Part Number: WPW10321304 — This part is available in TechHub. Order it and reschedule the call."

Show the agent's instructions (`technicianMessage`) below the resolution.

For rejected NLA tickets, show the same resubmit flow as standard rejections.

---

## IMPORTANT NOTES

- Do NOT modify the existing `/api/submissions/:id/process` route. NLA uses the new `/process-nla` route.
- `claimedAt` will be NULL for existing tickets — timing columns handle this gracefully.
- P-card check (`canOrderParts`) is ONLY enforced for `nla_part_found_vrs_ordered` and `nla_pcard_confirm` actions. All other NLA resolutions (replacement submitted, tech orders, reject, invalid) can be performed by any NLA-skilled agent regardless of P-card status.
- The escalation does NOT send any SMS to the technician — it's an internal handoff. The tech only gets notified when the P-card agent confirms and completes the order.
- Escalated tickets go back to `ticketStatus: "queued"` in the NLA queue so P-card agents can claim them. The `nlaEscalatedBy` field distinguishes them from fresh NLA tickets.
- Preserve all existing functionality: standard ticket processing, WebSocket notifications, Snowflake sync, SMS, photo handling, resubmission logic, notification sounds.
- Run `db:push` after schema changes.
- Update `replit.md` to document: NLA resolution types, P-card flag, escalation workflow, timing columns.