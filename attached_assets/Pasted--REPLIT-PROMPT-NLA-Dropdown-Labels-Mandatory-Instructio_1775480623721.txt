# REPLIT PROMPT — NLA Dropdown Labels + Mandatory Instructions + NLA Division Filtering

Three changes in this prompt:
1. Update NLA resolution options to match management-approved labels, add warranty-based replacement split, convert to dropdown, make instructions mandatory
2. Add appliance division filtering to NLA queues — NLA agents only see NLA tickets matching their assigned appliance divisions (same breakdown as standard VRS)
3. WebSocket and stats updates to support division-filtered NLA routing

---

## PART 1 — BACKEND: NEW REPLACEMENT ACTION + MANDATORY INSTRUCTIONS

### 1a. server/routes.ts — nlaProcessActionSchema (line 1359)

Add `"nla_replacement_tech_initiates"` to the action enum:

```ts
action: z.enum([
  "nla_replacement_submitted",
  "nla_replacement_tech_initiates",
  "nla_part_found_vrs_ordered",
  "nla_part_found_tech_orders",
  "nla_escalate_to_pcard",
  "nla_pcard_confirm",
  "nla_reject",
  "nla_invalid",
]),
```

### 1b. server/routes.ts — Enforce mandatory technicianMessage

Inside the `/api/submissions/:id/process-nla` route handler, after extracting parsed data (around line 1402), add this validation BEFORE the action-specific logic:

```ts
if (!technicianMessage || !technicianMessage.trim()) {
  return res.status(400).json({ error: "Instructions for technician are required for all NLA resolutions" });
}
```

This enforces it on every NLA action — approve, reject, invalid, escalate, everything.

### 1c. server/routes.ts — Add handler for nla_replacement_tech_initiates

After the existing `nla_replacement_submitted` handler block (after line 1424), add:

```ts
} else if (action === "nla_replacement_tech_initiates") {
  updateData.ticketStatus = "completed";
  updateData.statusChangedAt = new Date();
  updateData.reviewedBy = authReq.user!.id;
  updateData.reviewedAt = new Date();
  updateData.nlaResolution = "replacement_tech_initiates";
  updateData.technicianMessage = technicianMessage || null;

  smsMessage = `VRS NLA Update for SO#${submission.serviceOrder}\n\nStatus: NLA REPLACEMENT APPROVED\nThe part(s) you requested could not be sourced. VRS has approved a replacement.\n\nAction Required: You must initiate the replacement in TechHub. Follow standard replacement procedures in TechHub to process this replacement.`;
  if (technicianMessage) smsMessage += `\n\nInstructions: ${technicianMessage}`;
  smsType = "nla_replacement_tech_initiates";
```

---

## PART 2 — BACKEND: NLA DIVISION FILTERING

### 2a. server/storage.ts — Update getNlaQueuedCount to accept divisions

Current (line 1019):
```ts
async getNlaQueuedCount(): Promise<number> {
```

Change to:
```ts
async getNlaQueuedCount(divisions?: string[]): Promise<number> {
  const allApplianceDivisions = ["cooking", "dishwasher", "microwave", "laundry", "refrigeration", "hvac", "all_other"];
  const conditions: any[] = [
    eq(submissions.ticketStatus, "queued"),
    eq(submissions.requestType, "parts_nla"),
  ];
  // Filter by appliance type if agent has specific divisions (not all)
  if (divisions && divisions.length > 0) {
    const applianceDivisions = divisions.filter(d => d !== "nla" && d !== "generalist");
    const isApplianceGeneralist = applianceDivisions.length >= allApplianceDivisions.length;
    if (!isApplianceGeneralist && applianceDivisions.length > 0) {
      conditions.push(inArray(submissions.applianceType, applianceDivisions));
    }
  }
  const result = await db
    .select({ count: sql<number>`count(*)` })
    .from(submissions)
    .where(and(...conditions));
  return Number(result[0]?.count) || 0;
}
```

Also update the IStorage interface to match the new signature.

### 2b. server/storage.ts — Update getNlaCompletedTodayCount similarly

Add optional `divisions` parameter:
```ts
async getNlaCompletedTodayCount(agentId?: number, divisions?: string[]): Promise<number> {
```

Add the same appliance division filtering inside the method (same pattern as above — extract non-NLA divisions, check if generalist, add inArray condition if not).

### 2c. server/routes.ts — Pass divisions to NLA stats (line 1606)

Current:
```ts
if (divisions.includes("nla")) {
  nlaQueueCount = await storage.getNlaQueuedCount();
  nlaPendingCount = await storage.getNlaPendingCount(authReq.user!.id);
  nlaCompletedToday = await storage.getNlaCompletedTodayCount(authReq.user!.id);
}
```

Change to:
```ts
if (divisions.includes("nla")) {
  nlaQueueCount = await storage.getNlaQueuedCount(divisions);
  nlaPendingCount = await storage.getNlaPendingCount(authReq.user!.id);
  nlaCompletedToday = await storage.getNlaCompletedTodayCount(authReq.user!.id, divisions);
}
```

### 2d. server/storage.ts — NLA submissions query division filtering

In the `getSubmissionsWithTechnician` method, when `requestType === "parts_nla"` is in the filters AND a `divisionFilter` is provided, the existing `divisionFilter` logic already filters by `applianceType`. Verify this works: the `divisionFilter` condition pushes `inArray(submissions.applianceType, filters.divisionFilter)` which will correctly filter NLA tickets by their appliance type. If this condition is currently skipped when `requestType` is set, it needs to be applied regardless.

Check: make sure the `divisionFilter` condition is NOT skipped just because `requestType` is also set. Both filters should stack — `requestType === 'parts_nla' AND applianceType IN [agent's divisions]`.

---

## PART 3 — WEBSOCKET: NLA DIVISION-AWARE BROADCAST

### 3a. server/websocket.ts — Add broadcastToNlaDivisionAgents function

Add a new export function after `broadcastToDivisionAgents`:

```ts
export function broadcastToNlaDivisionAgents(applianceType: string, event: WSEvent, excludeUserId?: number) {
  for (const [userId, sessions] of clients) {
    if (excludeUserId && userId === excludeUserId) continue;
    const primary = sessions[0];
    if (!primary || primary.role !== "vrs_agent") continue;
    if (primary.agentStatus !== "online") continue;

    const hasNla = primary.divisions.includes("nla");
    if (!hasNla) continue;

    const allApplianceDivisions = Object.keys(DIVISION_LABELS).filter(k => k !== "nla");
    const agentApplianceDivisions = primary.divisions.filter((d: string) => d !== "nla" && d !== "generalist");
    const isApplianceGeneralist = agentApplianceDivisions.length >= allApplianceDivisions.length ||
      primary.divisions.includes("generalist");

    if (isApplianceGeneralist || agentApplianceDivisions.includes(applianceType)) {
      for (const c of sessions) {
        sendToClient(c.ws, event);
      }
    }
  }
}
```

This broadcasts NLA tickets only to agents who have BOTH the `"nla"` skill AND the matching appliance division. An NLA agent with only Refrigeration sees only fridge NLA notifications. An NLA agent with all appliance divisions sees all NLA notifications.

### 3b. server/routes.ts — Use new broadcast function for NLA tickets

Find all places where NLA tickets broadcast with `broadcastToDivisionAgents("nla", ...)`. Replace with `broadcastToNlaDivisionAgents(submission.applianceType || parsed.data.applianceType, ...)`.

Specifically:

**New submission broadcast (line 642):**
Current:
```ts
const broadcastDivision = parsed.data.requestType === "parts_nla" ? "nla" : parsed.data.applianceType;
broadcastToDivisionAgents(broadcastDivision, { ... });
```

Change to:
```ts
if (parsed.data.requestType === "parts_nla") {
  broadcastToNlaDivisionAgents(parsed.data.applianceType, {
    type: "new_ticket",
    payload: {
      submissionId: submission.id,
      serviceOrder: submission.serviceOrder,
      applianceType: parsed.data.applianceType,
      applianceLabel: getDivisionLabel(parsed.data.applianceType),
      warrantyLabel: getWarrantyLabel(parsed.data.warrantyType),
      requestType: parsed.data.requestType,
    },
  });
} else {
  broadcastToDivisionAgents(parsed.data.applianceType, {
    type: "new_ticket",
    payload: { ... },  // keep existing
  });
}
```

**Apply the same pattern to ALL other NLA broadcast calls** in the file (ticket claim, ticket release on reject, escalation broadcast, etc.). Anywhere the code currently does `broadcastToDivisionAgents("nla", ...)`, change to `broadcastToNlaDivisionAgents(submission.applianceType, ...)`.

Import `broadcastToNlaDivisionAgents` at the top of routes.ts alongside the existing websocket imports.

---

## PART 4 — AGENT DASHBOARD: SHOW DIVISION FILTERS FOR NLA VIEWS

### 4a. Un-hide division filters for NLA views

Find the wrapper that hides division filters for NLA (line 908):
```tsx
{!activeView.startsWith("nla_") && (
```

REMOVE this wrapper entirely (or change it to always show). The Division Filters sidebar group should be visible for BOTH standard and NLA views. When in NLA view, the division filter filters NLA tickets by appliance type.

### 4b. Pass division filter for NLA queries

In the query params builder (around line 423):

Current:
```ts
if (!isNlaView && divisionFilter) {
  params.set("applianceType", divisionFilter);
}
```

Change to:
```ts
if (divisionFilter) {
  params.set("applianceType", divisionFilter);
}
```

This applies the division filter to BOTH standard and NLA views.

### 4c. Keep Request Type filter hidden for NLA views

The request type filter should STAY hidden for NLA views (it's redundant — you're already looking at NLA-only tickets). The existing `if (!isNlaView && requestTypeFilter)` is correct. Leave it.

### 4d. Don't reset divisionFilter when switching to NLA views

Currently the NLA nav items reset `divisionFilter` to null:
```tsx
onClick={() => { setActiveView("nla_queue"); setSelectedId(null); setDivisionFilter(null); setRequestTypeFilter(null); }}
```

Change to NOT reset divisionFilter:
```tsx
onClick={() => { setActiveView("nla_queue"); setSelectedId(null); setRequestTypeFilter(null); }}
```

Do this for all three NLA nav items (nla_queue, nla_mytickets, nla_completed). This way, if an agent is filtering by Refrigeration and switches to the NLA queue, they still see only Refrigeration NLA tickets.

### 4e. NLA claim route — division check

In server/routes.ts, in the ticket claim route (around line 1040), the existing division check for NLA tickets currently only checks for `"nla"` skill:

```ts
const isNlaTicket = submission.requestType === "parts_nla";
if (!isGeneralist && !(isNlaTicket ? divisions.includes("nla") : divisions.includes(submission.applianceType))) {
```

Update to check BOTH NLA skill AND appliance division:
```ts
if (isNlaTicket) {
  if (!divisions.includes("nla")) {
    return res.status(403).json({ error: "You don't have the NLA specialization for this ticket" });
  }
  const agentApplianceDivisions = divisions.filter(d => d !== "nla" && d !== "generalist");
  const isApplianceGeneralist = agentApplianceDivisions.length >= ALL_DIVISIONS.filter(d => d !== "nla").length;
  if (!isApplianceGeneralist && !agentApplianceDivisions.includes(submission.applianceType)) {
    return res.status(403).json({ error: `You don't have the ${submission.applianceType} division to handle this NLA ticket` });
  }
} else if (!isGeneralist && !divisions.includes(submission.applianceType)) {
  return res.status(403).json({ error: "You don't have the division specialization for this ticket" });
}
```

Apply this same check in ALL places where NLA division access is verified (the process-nla route, the stage1 route if applicable, etc.).

---

## PART 5 — AGENT DASHBOARD: DROPDOWN UI WITH EXACT LABELS

### 5a. Replace card-style buttons with Select dropdown

In agent-dashboard.tsx, find the NLA resolution panel (around line 1885, the section with card buttons under "Part Not Found" and "Part Found" headers).

Replace the ENTIRE card-button section with a Select dropdown:

```tsx
<div className="space-y-2">
  <Label className="text-xs font-medium text-muted-foreground uppercase tracking-wider">
    Resolution *
  </Label>
  <Select
    value={nlaAction || ""}
    onValueChange={(val) => setNlaAction(val || null)}
  >
    <SelectTrigger data-testid="select-nla-resolution">
      <SelectValue placeholder="Select NLA resolution..." />
    </SelectTrigger>
    <SelectContent>
      <SelectItem value="nla_replacement_submitted">
        Approved Verified NLA VRS Replacement Submitted (SPHW / MPA)
      </SelectItem>
      <SelectItem value="nla_replacement_tech_initiates">
        Approved Verified NLA VRS Replacement Approved (SHW / Cinch — Tech initiates in TechHub)
      </SelectItem>
      <SelectItem value="nla_part_found_vrs_ordered">
        Approve Part Found Locally / Ordered (reschedule the call in TH)
      </SelectItem>
      <SelectItem value="nla_part_found_tech_orders">
        Approve Part Found (Instruct Tech to order part # Provided)
      </SelectItem>
      <SelectItem value="nla_reject">
        Rejected — More Information Needed
      </SelectItem>
      <SelectItem value="nla_invalid">
        Invalid NLA Request
      </SelectItem>
    </SelectContent>
  </Select>
</div>
```

### 5b. Keep conditional sub-fields after the dropdown

After the Select, show conditional fields based on selected value (part number input for tech_orders, rejection reasons for reject, invalid reason for invalid, P-card escalation note for vrs_ordered by non-P-card agent). Keep these exactly as they currently work — just move them below the dropdown instead of below the card buttons.

### 5c. Make Instructions for Technician mandatory

Change the label from:
```
Instructions for Technician (optional)
```
To:
```
Instructions for Technician *
```

Change the placeholder to:
```
Provide clear instructions for the technician. Required for all NLA resolutions.
```

### 5d. Add technicianMessage validation to submit buttons

In BOTH submit button onClick handlers ("Escalate to P-Card Agent" and "Process & Send NLA Resolution"), add BEFORE `setNlaConfirmOpen(true)`:

```ts
if (!technicianMessage.trim()) {
  toast({ title: "Instructions Required", description: "Please provide instructions for the technician.", variant: "destructive" });
  return;
}
```

### 5e. Disable submit buttons when instructions empty

Add `|| !technicianMessage.trim()` to the `disabled` prop on both buttons.

---

## PART 6 — UPDATE CONFIRMATION DIALOG + TECH DETAIL + EXPORTS

### 6a. Confirmation dialog — add new replacement option

In the confirmation dialog (around line 2990), add after the `nla_replacement_submitted` case:
```tsx
: nlaAction === "nla_replacement_tech_initiates" ? (
  <>Resolution: Approved Verified NLA VRS Replacement Approved. The technician will be notified to initiate the replacement in TechHub.</>
)
```

### 6b. Technician detail view — add new resolution display

In submission-detail.tsx, after the `replacement_submitted` case (around line 240), add:
```tsx
{sub.nlaResolution === "replacement_tech_initiates" && (
  <div>
    <p className="font-semibold text-emerald-700 dark:text-emerald-400">NLA Replacement Approved</p>
    <p className="text-sm text-muted-foreground mt-1">
      The part(s) could not be sourced. VRS has approved a replacement. You must initiate the replacement in TechHub using standard replacement procedures.
    </p>
  </div>
)}
```

### 6c. Exports — add new resolution label

Wherever resolution values are mapped to display labels in CSV/XLSX exports, add:
- `replacement_tech_initiates` → "Replacement Approved (Tech Initiates)"

### 6d. Escalated ticket display — add new resolution label

In the escalated ticket banner on agent-dashboard.tsx, add the new resolution to the label mapping:
```tsx
selectedSubmission.nlaResolution === "replacement_tech_initiates" ? "Replacement Approved — Tech Initiates" :
```

---

## IMPORTANT NOTES

- The `"nla"` skill PLUS appliance divisions together determine what an NLA agent can see. An agent with `["nla", "refrigeration", "cooking"]` sees only refrigeration and cooking NLA tickets. An agent with `["nla"]` plus all 7 appliance divisions sees all NLA tickets.
- Admin auto-assignment already gives all divisions (including `"nla"`), so admins see everything. No change needed there.
- The division filters in the sidebar now serve double duty — they filter standard tickets in standard views AND NLA tickets by appliance type in NLA views.
- Instructions are mandatory for EVERY NLA action. Backend enforces this as a safety net even if the frontend validation is bypassed.
- The dropdown labels are intentionally verbose — they match exactly what management specified. Do NOT shorten them.
- No schema migration needed — `nlaResolution` is a text column, new values work automatically.
- The new `broadcastToNlaDivisionAgents` function must be exported from websocket.ts and imported in routes.ts.
- Preserve all existing functionality.
- Update replit.md to document: NLA division-based routing, dropdown labels, mandatory instructions.