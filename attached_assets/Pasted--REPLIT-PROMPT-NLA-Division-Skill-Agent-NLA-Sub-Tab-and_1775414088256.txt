# REPLIT PROMPT — NLA Division Skill, Agent NLA Sub-Tab, and Export Separation

This prompt adds NLA as an assignable agent skill/division, creates a dedicated NLA section on the agent dashboard that keeps NLA tickets completely separate from the standard authorization queue, and upgrades the admin export to include NLA tickets on a separate sheet.

**CRITICAL: NLA tickets must NEVER appear in the standard Queue/My Tickets/Completed views, and standard tickets must NEVER appear in the NLA views. Complete separation.**

---

## PART 1 — ADD "nla" TO ALL DIVISION/SPECIALIZATION DEFINITIONS

There are SEVEN locations where the division list is defined. ALL SEVEN must be updated.

### 1a. server/routes.ts — ALL_DIVISIONS (line 57)

Current:
```ts
const ALL_DIVISIONS = ["cooking", "dishwasher", "microwave", "laundry", "refrigeration", "hvac", "all_other"];
```

Change to:
```ts
const ALL_DIVISIONS = ["cooking", "dishwasher", "microwave", "laundry", "refrigeration", "hvac", "all_other", "nla"];
```

### 1b. server/routes.ts — setSpecializationsSchema (line 2040–2042)

Current:
```ts
const setSpecializationsSchema = z.object({
  divisions: z.array(z.enum(["cooking", "dishwasher", "microwave", "laundry", "refrigeration", "hvac", "all_other"])),
});
```

Change to:
```ts
const setSpecializationsSchema = z.object({
  divisions: z.array(z.enum(["cooking", "dishwasher", "microwave", "laundry", "refrigeration", "hvac", "all_other", "nla"])),
});
```

### 1c. server/websocket.ts — DIVISION_LABELS (line 31–38)

Current:
```ts
const DIVISION_LABELS: Record<string, string> = {
  refrigeration: "Refrigeration",
  laundry: "Laundry",
  cooking: "Cooking",
  dishwasher: "Dishwasher / Compactor",
  microwave: "Microwave",
  hvac: "HVAC",
  all_other: "All Other",
};
```

Add to the end (before the closing brace):
```ts
  nla: "NLA Parts",
```

### 1d. client/src/pages/agent-dashboard.tsx — DIVISION_LABELS (line 101–108)

Add to the end:
```ts
  nla: "NLA Parts",
```

### 1e. client/src/pages/admin-dashboard.tsx — DIVISION_KEYS (line 160)

Current:
```ts
const DIVISION_KEYS = ["cooking", "dishwasher", "microwave", "laundry", "refrigeration", "hvac", "all_other"] as const;
```

Change to:
```ts
const DIVISION_KEYS = ["cooking", "dishwasher", "microwave", "laundry", "refrigeration", "hvac", "all_other", "nla"] as const;
```

### 1f. client/src/pages/admin-dashboard.tsx — DIVISION_LABELS (line 162–170)

Add to the end:
```ts
  nla: "NLA Parts",
```

### 1g. server/storage.ts — getQueuedCount (line 522)

This method has its own hardcoded allDivisions array:

Current:
```ts
const allDivisions = ["cooking", "dishwasher", "microwave", "laundry", "refrigeration", "hvac", "all_other"];
```

Change to:
```ts
const allDivisions = ["cooking", "dishwasher", "microwave", "laundry", "refrigeration", "hvac", "all_other", "nla"];
```

### 1h. IMPORTANT — Generalist logic

Adding `"nla"` to `ALL_DIVISIONS` means that when admins are auto-assigned all divisions, they get NLA too. This is correct — admins should see everything.

However, the "Generalist" check (`divisions.length >= ALL_DIVISIONS.length`) now requires 8 divisions instead of 7 to be considered a Generalist. This is actually CORRECT behavior — we do NOT want selecting all 7 appliance divisions to automatically grant NLA access. NLA must be explicitly assigned. A "Generalist" in the appliance sense (all 7 appliance types) should not automatically see NLA tickets.

BUT — the agent first-login division selection screen and the admin division checkbox grid will now show "NLA Parts" as a selectable option. This is correct. Admins can assign it to agents who handle parts.

---

## PART 2 — WEBSOCKET ROUTING FOR NLA TICKETS

### 2a. server/routes.ts — New submission broadcast (line 642)

Current:
```ts
broadcastToDivisionAgents(parsed.data.applianceType, {
  type: "new_ticket",
  payload: {
    submissionId: submission.id,
    serviceOrder: submission.serviceOrder,
    applianceType: parsed.data.applianceType,
    applianceLabel: getDivisionLabel(parsed.data.applianceType),
    warrantyLabel: getWarrantyLabel(parsed.data.warrantyType),
  },
});
```

Change to — route NLA tickets to agents with the `"nla"` specialization instead of by appliance type:
```ts
const broadcastDivision = parsed.data.requestType === "parts_nla" ? "nla" : parsed.data.applianceType;
broadcastToDivisionAgents(broadcastDivision, {
  type: "new_ticket",
  payload: {
    submissionId: submission.id,
    serviceOrder: submission.serviceOrder,
    applianceType: parsed.data.applianceType,
    applianceLabel: getDivisionLabel(parsed.data.applianceType),
    warrantyLabel: getWarrantyLabel(parsed.data.warrantyType),
    requestType: parsed.data.requestType,
  },
});
```

This ensures NLA notifications ONLY go to agents who have `"nla"` in their specializations.

### 2b. Check ALL other broadcastToDivisionAgents calls in routes.ts

There are additional broadcast calls at approximately lines 1064, 1315, 1566, and 1672. For each one, apply the same pattern: if the submission's `requestType === "parts_nla"`, use `"nla"` as the division key instead of `submission.applianceType`. Example:

```ts
const broadcastDivision = submission.requestType === "parts_nla" ? "nla" : submission.applianceType;
broadcastToDivisionAgents(broadcastDivision, { ... });
```

---

## PART 3 — AGENT DASHBOARD: QUEUE SEPARATION AND NLA SUB-TAB

### 3a. Add NLA activeView states

Add three new possible values for the `activeView` state: `"nla_queue"`, `"nla_mytickets"`, `"nla_completed"`.

### 3b. Add NLA navigation section to sidebar

After the existing "Request Type" SidebarGroup (around line 930), add a NEW SidebarGroup for NLA. This section should ONLY be visible to agents who have `"nla"` in their divisions (check `agentDivisions.includes("nla")`) OR who are admin/super_admin.

```tsx
{(agentDivisions.includes("nla") || isAdmin) && (
  <>
    <Separator className="my-2" />
    <SidebarGroup>
      <SidebarGroupLabel>NLA Parts</SidebarGroupLabel>
      <SidebarGroupContent>
        <SidebarMenu>
          <SidebarMenuItem>
            <SidebarMenuButton
              onClick={() => { setActiveView("nla_queue"); setSelectedId(null); setDivisionFilter(null); setRequestTypeFilter(null); }}
              data-active={activeView === "nla_queue"}
              data-testid="nav-nla-queue"
            >
              <Package className="w-4 h-4" />
              <span>NLA Queue</span>
              {statsData?.nlaQueueCount != null && activeView !== "nla_queue" && (
                <Badge variant="secondary" className="ml-auto text-xs bg-amber-100 text-amber-800 dark:bg-amber-900 dark:text-amber-200">
                  {statsData.nlaQueueCount}
                </Badge>
              )}
            </SidebarMenuButton>
          </SidebarMenuItem>
          <SidebarMenuItem>
            <SidebarMenuButton
              onClick={() => { setActiveView("nla_mytickets"); setSelectedId(null); setDivisionFilter(null); setRequestTypeFilter(null); }}
              data-active={activeView === "nla_mytickets"}
              data-testid="nav-nla-mytickets"
            >
              <Package className="w-4 h-4" />
              <span>My NLA Tickets</span>
              {statsData?.nlaPendingCount != null && statsData.nlaPendingCount > 0 && activeView !== "nla_mytickets" && (
                <Badge variant="secondary" className="ml-auto text-xs bg-amber-100 text-amber-800 dark:bg-amber-900 dark:text-amber-200">
                  {statsData.nlaPendingCount}
                </Badge>
              )}
            </SidebarMenuButton>
          </SidebarMenuItem>
          <SidebarMenuItem>
            <SidebarMenuButton
              onClick={() => { setActiveView("nla_completed"); setSelectedId(null); setDivisionFilter(null); setRequestTypeFilter(null); }}
              data-active={activeView === "nla_completed"}
              data-testid="nav-nla-completed"
            >
              <Package className="w-4 h-4" />
              <span>NLA Completed Today</span>
            </SidebarMenuButton>
          </SidebarMenuItem>
        </SidebarMenu>
      </SidebarGroupContent>
    </SidebarGroup>
  </>
)}
```

Import `Package` from `lucide-react` at the top of the file.

### 3c. Hide Division Filters and Request Type filters when in NLA views

When `activeView` starts with `"nla_"`, hide the "Division Filters" and "Request Type" SidebarGroups. NLA tickets are not filtered by appliance division or request type — they are ALL NLA by definition.

Wrap both existing SidebarGroups (Division Filters and Request Type) with:
```tsx
{!activeView.startsWith("nla_") && (
  // ... existing Division Filters and Request Type groups
)}
```

### 3d. Fetch logic — separate NLA and standard queries

The `useQuery` hook that fetches submissions for the agent queue needs to add request type filtering. Update the query parameter construction (around where `params` is built based on `activeView`):

For standard views (`queue`, `mytickets`, `completed`):
- Add `params.set("excludeRequestType", "parts_nla")` to exclude NLA tickets

For NLA views (`nla_queue`, `nla_mytickets`, `nla_completed`):
- Add `params.set("requestType", "parts_nla")` to include ONLY NLA tickets
- For `nla_queue`: fetch queued NLA tickets (same pattern as `queue` but filtered to NLA)
- For `nla_mytickets`: fetch pending NLA tickets assigned to this agent (same pattern as `mytickets` but filtered to NLA)
- For `nla_completed`: fetch completed NLA tickets for today (same pattern as `completed` but filtered to NLA)

### 3e. Backend: Support excludeRequestType filter

In `server/routes.ts`, in the `GET /api/submissions` handler, add support for `excludeRequestType` query parameter:

```ts
if (req.query.excludeRequestType) {
  // This will be handled in the storage layer or as a post-filter
}
```

The cleanest approach: pass `excludeRequestType` through to the storage query. In `server/storage.ts`, update the `getSubmissionsWithTechnician` method's filters interface to accept `excludeRequestType?: string`, and add a condition:

```ts
if (filters.excludeRequestType) {
  conditions.push(sql`${submissions.requestType} != ${filters.excludeRequestType}`);
}
```

Do the same for `getSubmissions` if it's used for the queue.

### 3f. Header title update

Update the header `<h1>` to show appropriate titles for NLA views:
```tsx
{activeView === "nla_queue" && `NLA Parts Queue (${statsData?.nlaQueueCount ?? 0})`}
{activeView === "nla_mytickets" && `My NLA Tickets (${statsData?.nlaPendingCount ?? 0})`}
{activeView === "nla_completed" && "NLA Completed Today"}
```

### 3g. SHSAI panel suppression

When viewing an NLA ticket (where `selectedSubmission?.requestType === "parts_nla"`), do NOT render the SHSAI service order lookup panel. SHSAI is not applicable for NLA tickets. Wrap the SHSAI section with:
```tsx
{selectedSubmission?.requestType !== "parts_nla" && (
  // ... existing SHSAI panel
)}
```

---

## PART 4 — AGENT STATS: NLA COUNTS

### 4a. server/routes.ts — Agent stats endpoint (line 1340)

Update the agent stats endpoint to return NLA-specific counts. After computing existing counts, add:

```ts
// NLA stats (only if agent has NLA specialization)
let nlaQueueCount = 0;
let nlaPendingCount = 0;
let nlaCompletedToday = 0;

if (divisions.includes("nla") || userRole === "admin" || userRole === "super_admin") {
  nlaQueueCount = await storage.getNlaQueuedCount();
  nlaPendingCount = await storage.getNlaPendingCount(authReq.user!.id);
  nlaCompletedToday = await storage.getNlaCompletedTodayCount(authReq.user!.id);
}

return res.status(200).json({ queueCount, pendingCount, completedToday, nlaQueueCount, nlaPendingCount, nlaCompletedToday });
```

### 4b. server/storage.ts — Add NLA count methods

Add three new methods to the DatabaseStorage class and the IStorage interface:

```ts
async getNlaQueuedCount(): Promise<number> {
  const result = await db
    .select({ count: sql<number>`count(*)` })
    .from(submissions)
    .where(and(
      eq(submissions.ticketStatus, "queued"),
      eq(submissions.requestType, "parts_nla")
    ));
  return Number(result[0]?.count) || 0;
}

async getNlaPendingCount(agentId: number): Promise<number> {
  const result = await db
    .select({ count: sql<number>`count(*)` })
    .from(submissions)
    .where(and(
      eq(submissions.ticketStatus, "pending"),
      eq(submissions.requestType, "parts_nla"),
      eq(submissions.assignedTo, agentId)
    ));
  return Number(result[0]?.count) || 0;
}

async getNlaCompletedTodayCount(agentId?: number): Promise<number> {
  const today = new Date();
  today.setHours(0, 0, 0, 0);
  const conditions: any[] = [
    eq(submissions.requestType, "parts_nla"),
    gte(submissions.statusChangedAt, today),
    inArray(submissions.ticketStatus, ["completed", "approved"]),
  ];
  if (agentId) {
    conditions.push(eq(submissions.reviewedBy, agentId));
  }
  const result = await db
    .select({ count: sql<number>`count(*)` })
    .from(submissions)
    .where(and(...conditions));
  return Number(result[0]?.count) || 0;
}
```

### 4c. Exclude NLA from standard queue count

Update the existing `getQueuedCount` method (line 522 in storage.ts) to EXCLUDE NLA tickets from the standard queue count. Add this condition:

```ts
conditions.push(sql`${submissions.requestType} != 'parts_nla'`);
```

This ensures the standard Queue badge count does not include NLA tickets.

Similarly, update `getPendingCount` and `getCompletedTodayCount` to exclude NLA tickets by adding the same condition.

---

## PART 5 — ADMIN DASHBOARD: NLA FILTERING

### 5a. Ticket Overview — request type filter

The admin ticket overview already shows NLA tickets with a "Parts — NLA" label and part numbers. Add a request type filter row above or below the existing status filter tabs.

Add a simple toggle group:
```
Show: [All Tickets] [Authorization Only] [NLA Only]
```

This filters `allTickets` by `requestType` before the existing status filter is applied. Default to "All Tickets".

### 5b. Analytics — NLA summary

In the Analytics tab, add a small summary card below the existing volume metrics:

```
NLA Parts Submissions
Today: X | This Week: X | This Month: X | All Time: X
```

This requires adding NLA-filtered counts to the analytics endpoint, or computing them client-side from the existing data.

---

## PART 6 — EXPORT: XLSX WITH SEPARATE NLA SHEET

### 6a. Install exceljs

```bash
npm install exceljs
```

### 6b. Modify the export route (server/routes.ts line 2104)

Update the existing `/api/admin/export-csv` route to support a `format` query parameter:

- If `format=xlsx` → return an XLSX file with two sheets
- If `format=csv` or omitted → return existing CSV behavior (backwards compatible)

**XLSX behavior:**

Import exceljs at the top of routes.ts:
```ts
import ExcelJS from "exceljs";
```

When `format === "xlsx"`:

1. Split `allSubmissions` into two arrays:
   - `standardTickets` = submissions where `requestType !== "parts_nla"`
   - `nlaTickets` = submissions where `requestType === "parts_nla"`

2. Create a workbook with two sheets:
   - **Sheet 1: "Authorization Tickets"** — `standardTickets` with the same columns as the current CSV
   - **Sheet 2: "NLA Parts Tickets"** — `nlaTickets` with the same columns PLUS an additional "Part Numbers" column (parse the `partNumbers` JSON field and display as comma-separated values)

3. Return the workbook:
```ts
const workbook = new ExcelJS.Workbook();

// Sheet 1 — Standard tickets
const sheet1 = workbook.addWorksheet("Authorization Tickets");
sheet1.columns = [
  { header: "ID", key: "id", width: 8 },
  { header: "Service Order", key: "serviceOrder", width: 16 },
  { header: "Technician LDAP", key: "techLdap", width: 15 },
  { header: "Technician Name", key: "techName", width: 20 },
  { header: "Phone", key: "phone", width: 15 },
  { header: "District", key: "district", width: 10 },
  { header: "Appliance Type", key: "applianceType", width: 18 },
  { header: "Request Type", key: "requestType", width: 18 },
  { header: "Warranty Type", key: "warrantyType", width: 15 },
  { header: "Warranty Provider", key: "warrantyProvider", width: 20 },
  { header: "Issue Description", key: "description", width: 40 },
  { header: "Estimate Amount", key: "estimate", width: 15 },
  { header: "Stage 1 Status", key: "stage1Status", width: 15 },
  { header: "Stage 1 Reviewed By", key: "stage1Reviewer", width: 20 },
  { header: "Stage 1 Reviewed At", key: "stage1ReviewedAt", width: 22 },
  { header: "Rejection Reasons", key: "rejectionReasons", width: 30 },
  { header: "Stage 2 Status", key: "stage2Status", width: 15 },
  { header: "Stage 2 Reviewed By", key: "stage2Reviewer", width: 20 },
  { header: "Stage 2 Reviewed At", key: "stage2ReviewedAt", width: 22 },
  { header: "Auth Code", key: "authCode", width: 15 },
  { header: "RGC Code", key: "rgcCode", width: 15 },
  { header: "Assigned To", key: "assignedTo", width: 20 },
  { header: "Created At", key: "createdAt", width: 22 },
  { header: "Updated At", key: "updatedAt", width: 22 },
];
// Add rows from standardTickets...

// Sheet 2 — NLA tickets
const sheet2 = workbook.addWorksheet("NLA Parts Tickets");
// Same columns as sheet1 PLUS:
//   { header: "Part Numbers", key: "partNumbers", width: 30 },
// Add rows from nlaTickets, parsing partNumbers JSON into comma-separated string
```

4. Send response:
```ts
const buffer = await workbook.xlsx.writeBuffer();
const filename = `vrs-tickets-${rangeLabel}-${now.toISOString().slice(0, 10)}.xlsx`;
res.setHeader("Content-Type", "application/vnd.openxmlformats-officedocument.spreadsheetml.spreadsheet");
res.setHeader("Content-Disposition", `attachment; filename="${filename}"`);
return res.status(200).send(Buffer.from(buffer as ArrayBuffer));
```

### 6c. Admin dashboard export UI (admin-dashboard.tsx)

In the export section of the Analytics tab, add a format selector:

```tsx
<div className="flex gap-2">
  <Button onClick={() => handleExport("csv")} variant="outline" size="sm">
    Export CSV
  </Button>
  <Button onClick={() => handleExport("xlsx")} size="sm">
    Export XLSX (separate NLA sheet)
  </Button>
</div>
```

Update the export handler to pass `format` as a query parameter:
```ts
params.set("format", format); // "csv" or "xlsx"
```

For XLSX, update the download filename detection to use `.xlsx` extension and the correct content type.

---

## IMPORTANT NOTES

- Do NOT modify technician-facing pages — they are complete.
- Do NOT modify the NLA ticket action/resolution panel — it currently uses the same Approve/Reject/Invalid flow as standard tickets. Custom NLA resolution options will come in a separate prompt.
- The `"nla"` specialization is INDEPENDENT of appliance divisions. Selecting all 7 appliance types makes an agent a "Generalist" for appliance tickets. NLA must be assigned separately and explicitly. An agent can have `["refrigeration", "nla"]` — they see refrigeration tickets in Queue and NLA tickets in NLA Queue.
- Admins and super_admins automatically get ALL divisions (including NLA) via the existing `ALL_DIVISIONS` auto-assignment.
- NLA tickets still have an `applianceType` (set by the tech on the form), but routing is by the `"nla"` specialization, NOT by appliance type.
- Preserve all existing functionality: WebSocket notifications, Snowflake sync, SHSAI integration (suppressed for NLA only), SMS, photo handling, resubmission logic, notification sounds.
- Run `db:push` after any schema changes if needed (this prompt primarily changes application logic, not database schema).
- Update `replit.md` to document: NLA skill assignment, NLA sub-tab on agent dashboard, XLSX export with NLA sheet, NLA queue separation.