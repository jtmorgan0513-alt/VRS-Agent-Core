Add SHSAI integration to VRS Agent ticket detail view — Stage 2 ONLY:
1. Location:

Stage 2 ONLY — After ticket has been approved in Stage 1
Do NOT show SHSAI panel in Stage 1
Use the empty white space to the RIGHT of the ticket detail panel
Split view: Ticket details (60%) | SHSAI Panel (40%)

2. Fresh Session on Each Ticket:

Every time a VRS agent opens/selects a ticket in Stage 2:

Refresh the SHSAI iframe completely (destroy and recreate)
Start a new session
Re-initialize with agent's LDAP ID
Auto-send the prompt with the current ticket's service order


This ensures each ticket gets a clean SHSAI session — no carryover from previous tickets

3. Implementation:
javascript// When agent clicks on a Stage 2 ticket:
function loadSHSAI(serviceOrder, agentLdapId) {
  // Remove existing iframe to clear session
  const container = document.getElementById('shsai-container');
  container.innerHTML = '<div class="loading">Loading SHSAI...</div>';
  
  // Create fresh iframe
  const iframe = document.createElement('iframe');
  iframe.src = `https://routing.uat.tellurideplatform.com/?t=${Date.now()}`; // Cache-bust
  iframe.id = 'shsai-frame';
  iframe.width = '100%';
  iframe.height = '500px';
  
  iframe.onload = () => {
    // After iframe loads, inject the prompt
    // Auto-send: "Give me all orders for customer having sample service order number {service_order}"
  };
  
  container.innerHTML = '';
  container.appendChild(iframe);
}
4. Auto-Prompt on Load:

After iframe loads, automatically send:

"Give me all orders for customer having sample service order number {service_order}"
Example: "Give me all orders for customer having sample service order number 8175-04521789"



5. Quick Action Buttons (Stage 2 only):

Order Info — "Give me order info for {service_order}"
Customer Info — "Give me customer info for {service_order}"
All Orders for Customer — "Give me all orders for customer having service order number {service_order}"
Protection Agreement — "Show protection agreement for {service_order}"
Warranty — "Show warranty info for {service_order}"
Pre-call Brief — "Give me pre-call brief for {service_order}"
Clicking any button refreshes iframe and sends that specific prompt

6. Stage 1 View:

NO SHSAI panel
Ticket detail takes full width (current behavior)
Agent reviews submission, approves or rejects

7. Stage 2 View:

SHSAI panel appears on right side
Auto-loads fresh session when ticket is selected
Agent can review service history while entering auth code

8. Switching Between Tickets:

When agent clicks a different ticket in Stage 2 queue:

Completely refresh SHSAI iframe
New session
New auto-prompt with the new service order


No session bleed between tickets

9. Collapse Option:

Toggle to hide SHSAI panel if agent doesn't need it
Button: "Hide Service History" / "Show Service History"
Panel state remembered per session (not per ticket)"