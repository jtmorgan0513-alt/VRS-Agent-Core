"Add super admin account, password reset functionality, and in-app navigation:
1. Super Admin Account (Master Login):

Create a protected super admin account:

LDAP ID: VRS_MASTER
Name: System Administrator
Role: super_admin (new role, above admin)
Password: VRS_Master_2026! (strong default — should be changed)
must_change_password = false
is_system_account = true


Protection rules:

Cannot be deleted
Cannot be deactivated
Cannot have role changed
Does NOT appear in user list (hidden from regular admin view)
Only visible when logged in AS the super admin


Super admin capabilities:

All admin functions
Can reset ANY user's password (including other admins)
Can create/modify/delete admins
Can view system settings
Can see audit logs (future)



2. Password Reset by Admin/Super Admin:

On User Management page, each user row has:

"Reset Password" button
Clicking resets password to VRS2026!
Sets must_change_password = true
Shows confirmation: "Password reset. User must change on next login."


Permissions:

Super Admin: Can reset ANY user (including admins)
Admin: Can reset VRS Agents only (not other admins)



3. Phone Number for Password Reset Link (Self-Service):

Add phone field to admin/agent user profiles
Admin can enter/update phone number for each user
On login page, add: "Forgot Password?" link
Forgot password flow:

User enters LDAP ID
If phone number on file, send SMS via Twilio with reset link/code
Reset link contains one-time token (expires in 15 minutes)
User clicks link → enters new password
Password updated, must_change_password = false


Database:

Add password_reset_token VARCHAR(100)
Add password_reset_expires TIMESTAMP


SMS message:
VRS Password Reset
Your reset code is: {6-digit-code}
Or click: {reset_link}
Expires in 15 minutes.


4. In-App Back Navigation:

Add a persistent navigation header on ALL pages (tech, agent, admin)
Header includes:

"← Back" button (left side)
Page title (center)
User info / logout (right side)


Back button behavior:

Tracks navigation history within the app (not relying on browser)
Works in PWA / desktop app where browser back doesn't exist
Uses React Router history or custom navigation stack


Implementation:
javascript// Navigation context to track history
const [navHistory, setNavHistory] = useState([]);

function navigateTo(path) {
  setNavHistory([...navHistory, currentPath]);
  router.push(path);
}

function goBack() {
  if (navHistory.length > 0) {
    const previousPath = navHistory.pop();
    setNavHistory([...navHistory]);
    router.push(previousPath);
  } else {
    // Go to home/dashboard
    router.push('/dashboard');
  }
}

Show breadcrumbs where helpful:

Dashboard > Stage 2 > SO# 8175-12345678


On root pages (home/dashboard), hide back button or show home icon instead
Keyboard shortcut: Alt + ← for back navigation on desktop

5. Navigation Header Layout:
┌─────────────────────────────────────────────────────────┐
│ [← Back]     Stage 2 - Authorization          [Logout] │
└─────────────────────────────────────────────────────────┘

Mobile (technician app):

┌─────────────────────────────────────────────────────────┐
│ [←]          New Submission                    [menu]  │
└─────────────────────────────────────────────────────────┘
6. Test scenarios:

Super admin can reset an admin's password
Admin can reset a VRS agent's password but NOT another admin's
User can request password reset via SMS if phone on file
Back button works when navigating between pages in PWA/desktop app
Back button returns to previous page, not just home"