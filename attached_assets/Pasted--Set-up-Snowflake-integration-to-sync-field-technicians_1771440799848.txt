"Set up Snowflake integration to sync field technicians using key-pair authentication:
1. Install Snowflake Connector:
bashnpm install snowflake-sdk
2. Environment Secrets (already linked to app):
SNOWFLAKE_ACCOUNT=sears_hs_prod.us-east-1
SNOWFLAKE_USERNAME=SVC_SCA_AUTO
SNOWFLAKE_WAREHOUSE=SCIENTIST_PRD_WH
SNOWFLAKE_PRIVATE_KEY=(private key content)
3. Create Snowflake Service:

Create /server/services/snowflake.js
Use key-pair authentication with authenticator: 'SNOWFLAKE_JWT'
Database: PRD_TPMS
Schema: HSTECH

4. Technician Sync Query:
sqlWITH filtered_techs AS (
  SELECT 
    TECH_UN_NO,
    TECH_AIM_ID,
    LDAP_ID,
    FIRST_NM,
    LAST_NM,
    MBL_PH_NO,
    DIST_UN_NO,
    TECH_STS_CD,
    ACTIVE_IND,
    MGR_NM
  FROM PRD_TPMS.HSTECH.COMTTU_TECH_UN
  WHERE MBL_PH_NO IS NOT NULL
    AND MBL_PH_NO <> ' '
    AND TECH_STS_CD = 'A'
    AND ACTIVE_IND = 'Y'
),
primary_tech_assignment AS (
  SELECT 
    t.*,
    ROW_NUMBER() OVER (PARTITION BY LDAP_ID ORDER BY TECH_UN_NO) AS primary_rank
  FROM filtered_techs t
)
SELECT 
  p.LDAP_ID AS TECH_ID,
  p.FIRST_NM || ' ' || p.LAST_NM AS TECH_NAME,
  p.MBL_PH_NO AS PHONE_NUMBER,
  p.MGR_NM,
  p.TECH_UN_NO,
  p.DIST_UN_NO AS DISTRICT_ID
FROM primary_tech_assignment p
WHERE p.primary_rank = 1
ORDER BY p.LDAP_ID
5. Technicians Table Schema:
sqlCREATE TABLE technicians (
  id SERIAL PRIMARY KEY,
  ldap_id VARCHAR(50) UNIQUE NOT NULL,
  name VARCHAR(255),
  phone VARCHAR(20),
  district VARCHAR(10),
  manager_name VARCHAR(255),
  tech_un_no VARCHAR(50),
  is_active BOOLEAN DEFAULT true,
  last_synced_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
6. Field Mapping:
Snowflake FieldLocal FieldTECH_ID (LDAP_ID)ldap_idTECH_NAMEnamePHONE_NUMBERphoneDISTRICT_IDdistrictMGR_NMmanager_nameTECH_UN_NOtech_un_no
7. Sync Endpoint:

POST /api/admin/sync-technicians (Admin only)
Connects to Snowflake using key-pair auth
Runs the query
Upserts into local technicians table:

If LDAP_ID exists → update fields
If LDAP_ID is new → insert
If LDAP_ID no longer in Snowflake results → set is_active = false


Returns: { synced: X, added: X, updated: X, deactivated: X }

8. Admin UI:

Add "Sync Technicians" section in Admin dashboard
Shows last sync time
Shows total active technicians count
"Sync Now" button with progress indicator
Results display after sync completes

9. Technician Login Flow (No Password):

Tech enters LDAP ID only (e.g., jmorga1)
Lookup in technicians table where is_active = true
If found:

Show: "Welcome, {name}"
Pre-fill district: {district}
Phone on file: {phone} (masked as ***-**XX)
Checkbox: "My phone number has changed" (if checked, show editable phone field)


If not found or inactive:

Show: "ID not found or inactive. Please contact your manager."


No password required for technicians

10. Submission Linked to Technician:

Store ldap_id on submissions (not user_id)
Store phone_override if tech changed their phone
SMS sends to phone_override if present, otherwise technicians.phone

11. Technician History:

After LDAP ID entry, show option: "View previous submissions"
Displays all past submissions for that LDAP ID

12. Keep VRS Agent/Admin Login Separate:

VRS Agents and Admins still use email + password via users table
Technicians use technicians table with LDAP ID only
These are separate login flows"



This should get the full Snowflake sync and technician login working. Let me know how it goes! 🚀